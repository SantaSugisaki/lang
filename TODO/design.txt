型は明示的に宣言する(型推論なし)
go言語のようなメソッドを実装する(オブジェクト指向はあんまりやらない)
セミコロンは１行に2文書きたいときにのみ書けるようにする
基本的にはc言語やc++とgo言語が混ざったような文法にする(型の名前はgo言語と同じような名前にする)(ただしメモリは触らせない)
foreach文も実装する
インタプリンタ言語を作る
虚数の計算もできるようにする
go言語で全部実装する　※自分が慣れているから。アルファベット以外の変数名も使えるようにしたいから。
func () 関数名 (引数)
{
 return
ここは型推論する※,で複数の返り値を持てるようにする(型を厳格にしておくことで、ここの型は一つにおそらく定まるという希望的観測)
}
if式にする
競技プログラミングのための言語にしたい
queue,stack,priority_queueなどの構造体を実装する
再帰関数は、自動的にfor文のように変換されるようにする。
